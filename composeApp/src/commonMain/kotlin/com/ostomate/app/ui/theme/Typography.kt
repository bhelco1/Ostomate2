package com.ostomate.app.ui.theme

import androidx.compose.material3.Typography

// M3 default type ramp — platform fonts (Roboto on Android, SF Pro on iOS) via CMP.
// Respects OS font scaling including 200 % accessibility size.
val OstomateTypography = Typography()
